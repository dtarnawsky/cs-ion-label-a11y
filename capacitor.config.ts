import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.ion.label.a11y',
  appName: 'cs-ion-label-a11y',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
